name: Build Dev Docker Image

on:
  push:
    branches:
      - dev
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v6

      - name: Set image name
        run: |
          echo "IMAGE_NAME=ghcr.io/${GITHUB_REPOSITORY,,}" >> "$GITHUB_ENV"

      - name: Login to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build and push full image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile
          target: production
          platforms: linux/amd64
          push: true
          tags: ${{ env.IMAGE_NAME }}:dev
          cache-from: type=gha,scope=deeptutor-dev-full
          cache-to: type=gha,mode=max,scope=deeptutor-dev-full

      - name: Build and push query-only image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile
          target: production-query
          platforms: linux/amd64
          push: true
          tags: ${{ env.IMAGE_NAME }}:dev-query
          cache-from: |
            type=gha,scope=deeptutor-dev-query
            type=gha,scope=deeptutor-dev-full
          cache-to: type=gha,mode=max,scope=deeptutor-dev-query
